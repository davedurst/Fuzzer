"""
    Fuzzer Project
    SWEN-331
    Group 4
"""

def test(param_dict, form_dict, args):
  """ access the global session """
  global sess
  
  #read vectors
  
  #attack forms
  
  #attack url params

def test_forms(form_dict, vectors, session, args.random, args.timeout):
    #attack all inputs with vectors
    return
  
def test_url_param(param_dict, vectors, session, args.random, args.timeout):
    #attack all url params with vectors
    return
